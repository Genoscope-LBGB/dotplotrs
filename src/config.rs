pub struct Config {
    pub paf: String,
    pub min_aln_size: u64,
    pub height: u32,
    pub width: u32,
    pub margin_x: f32,
    pub margin_y: f32,
    pub font_size: u32,
    pub output: String,
    pub debug: bool,
    pub line_thickness: u32,
}
